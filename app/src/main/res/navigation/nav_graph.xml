<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/mainActivity">

    <activity
        android:id="@+id/mainActivity"
        android:name="com.example.SimpleCalculator.MainActivity"
        android:label="activity_main"
        tools:layout="@layout/activity_main" />
    <fragment
        android:id="@+id/colorSet"
        android:name="com.example.SimpleCalculator.ColorSet"
        android:label="fragment_color_set"
        tools:layout="@layout/fragment_color_set" >
        <action
            android:id="@+id/action_colorSet_to_mainActivity"
            app:destination="@id/mainActivity" />
    </fragment>
</navigation>